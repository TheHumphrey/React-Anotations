import auth from './requests/authService';
import { IAuth, ISessao, IUserResponse, IUsuario } from './requests/types/types';

export default auth;

export type { IAuth, ISessao, IUserResponse, IUsuario };
